const sinon = require('sinon');
const should = require('should');

describe('Bucketlist Controller', function () {
  describe('#post', function () {
    it('should create a new Bucketlist', function () {

    });

    it('should not create without a title');
  })
})
